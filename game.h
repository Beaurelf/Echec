#ifndef GAME_H
#define GAME_H

#include <QMainWindow>
#include "home.h"

class Game : public QMainWindow
{
    Q_OBJECT
private:

public:
    Game(QWidget *parent = nullptr);
    ~Game();

public slots:

};
#endif // GAME_H
